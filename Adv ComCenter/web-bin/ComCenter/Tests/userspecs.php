<?PHP
$user = $u0000spidey;
$paragraph = "";
$totalposts = "0";
$totaltopics = "0";
$totalpolls = "0";
if ($totalposts != "0") {
$postlength = (0)/$totalposts;
}
if ($totalposts == 0) {
$postlength = 0;
}
$lastpostdate = "";
if ($lastpostdate == "") {
$lastpostdate = "User has not yet posted";
}
$signedupdate = "";
if ($signedupdate == "") {
$signedupdate = "Not Available";
}
$lastvisitdate = "User has not yet visited the forum";
?>